name: Test Spack Build-Cache Action

on:
  push:
  pull_request:

env:
  spack_env: .spack/default

jobs:
  smoke:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Spack Build-Cache
        uses: ./
        with:
          environment: ${{ env.spack_env }}

      - name: Check specs
        run: |
          spack spec

      - name: Check programs
        run: |
          which -a cmake
